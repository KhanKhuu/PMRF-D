/**
  Header for the remove duplicates function.
*/

#ifndef REMOVE_DUPLICATES_HPP
#define REMOVE_DUPLICATES_HPP

/**
 Removes duplicate elements in the binary image data vector.

 @param data stores the input image's data
*/
void removeDuplicates(std::vector< std::pair< uint8_t, size_t > > *data);

#endif //REMOVE_DUPLICATES_HPP
