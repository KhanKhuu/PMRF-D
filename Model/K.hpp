#ifndef K_hpp
#define K_hpp

struct Constants {
    double DEFAULT_LAMBDA = 0.5;
    int DEFAULT_PSI_MAX = 5000;
    size_t DEFAULT_NUM_THREADS = 1;
    size_t DEFAULT_NUM_ITER = 1;
};

Constants K;

#endif /* K_hpp */
